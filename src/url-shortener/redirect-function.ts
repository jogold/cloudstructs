// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for RedirectFunction
 */
export interface RedirectFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/url-shortener/redirect.
 */
export class RedirectFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: RedirectFunctionProps) {
    super(scope, id, {
      description: 'src/url-shortener/redirect.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/url-shortener/redirect.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}